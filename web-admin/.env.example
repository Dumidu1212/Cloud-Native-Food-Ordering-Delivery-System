# Base URL for API requests (used by the admin portal to communicate with the backend)
REACT_APP_API_BASE_URL=https://api.yourdomain.com

# Specify the environment (e.g., development, staging, production)
REACT_APP_ENV=production
